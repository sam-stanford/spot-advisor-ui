import { AdvisorWeights } from '../api/schema/Advisor';

const DefaultCustomAdvisoWeights: AdvisorWeights = {
  availability: 0.33,
  performance: 0.33,
  price: 0.33,
};
export default DefaultCustomAdvisoWeights;
