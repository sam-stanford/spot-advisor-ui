import Options from '../api/schema/Options';

const DefaultOptions: Options = {
  avoidRepeatedInstanceTypes: true,
  shareInstancesBetweenApplications: true,
  regions: [],
};
export default DefaultOptions;
